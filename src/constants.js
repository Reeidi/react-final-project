export const baseUrl = 'http://localhost:3946';

export const registerUserUrl = `${baseUrl}/auth/register`;
export const loginUserUrl = `${baseUrl}/auth/login`;
export const logoutUserUrl = `${baseUrl}/auth/logout`;